
async function loadArticles(){const res=await fetch('/data/articles.json?v=132');return await res.json();}
function storyCard(a){return `<a class="card story-card" href="${a.url}"><div class="cover" style="background-image:url('${a.cover}')"></div><div class="card-body"><span class="chip">${a.series||a.category}</span><h3>${a.title}</h3><p class="muted">${a.summary}</p><span class="text-link">${a.readingTime}閱讀</span></div></a>`}
function listCard(a){return `<a class="list-card" href="${a.url}"><div class="list-thumb" style="background-image:url('${a.cover}')"></div><div><span class="chip">${a.series||a.category}</span><h3>${a.title}</h3><p class="muted">${a.summary}</p><p class="muted">${a.date}｜${a.readingTime}</p><span class="text-link">在本站閱讀 →</span></div></a>`}
async function renderHome(){const picks=document.querySelector('[data-picks]');if(!picks)return;const data=await loadArticles();picks.innerHTML=data.filter(a=>a.featured).slice(0,4).map(storyCard).join('');}
async function renderList(){const el=document.querySelector('[data-list]');const input=document.querySelector('[data-search]');if(!el)return;const data=await loadArticles();function r(q=''){q=q.trim().toLowerCase();const filtered=data.filter(a=>(`${a.title} ${a.category} ${a.series} ${a.character} ${a.summary} ${(a.tags||[]).join(' ')}`).toLowerCase().includes(q));el.innerHTML=filtered.map(listCard).join('')||'<p class="muted">找不到符合的故事。</p>'}r(); if(input) input.addEventListener('input',e=>r(e.target.value));}
renderHome();renderList();
