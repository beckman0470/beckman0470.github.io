# 首頁手動修改重點

如果你不想跑工具，至少要在 `index.html` 內把這些改掉：

```html
aria-label="前往獨家記憶"
```

改成：

```html
aria-label="前往家庭誌"
```

```html
alt="獨家記憶插圖：..."
```

改成：

```html
alt="家庭誌插圖：..."
```

```html
<h3>獨家記憶</h3>
```

改成：

```html
<h3>家庭誌</h3>
```

如果上方列或其他頁面還有「獨家記憶」，也全部改成「家庭誌」。
