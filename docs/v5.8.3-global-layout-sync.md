# v5.8.3｜Global Layout Sync

## 目的

修正首頁與文章頁 Header / Footer 不一致，避免從首頁點進文章後標題、Logo、導覽列跳版。

## 修改策略

- 掃描所有正式 HTML 頁面。
- 跳過 `templates/` 內的 component。
- 跳過疑似 Markdown / 非完整 HTML 的檔案。
- 將所有正式頁面的 `<header>` 與 `<footer>` 替換成同一套全域版型。
- 新增 `css/global-layout-sync.css` 統一 Header / Footer 樣式。

## 修改檔案數量

```text
HTML modified: 37
Skipped: 0
Self-check issues: 0
```

## Skipped

```text
無
```

## Self-check

```text
通過：所有修改頁面皆有 1 個 Header、1 個 Footer、全域 CSS，以及完整導覽項目。
```

## 上傳方式

這是 Patch ZIP，只包含修改過的檔案。  
請將 ZIP 內檔案依照路徑覆蓋到 GitHub Repository。
