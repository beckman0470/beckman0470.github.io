# v2.4 Stories Page JSON Reader

## 覆蓋 / 新增

- articles.html
- js/stories-page.js

## 功能

- `articles.html` 讀取 `data/stories.json`
- 自動產生推薦閱讀
- 自動產生全部故事
- 支援分類篩選
- 支援前端搜尋
- JSON 失敗時使用 fallback stories，頁面不會空白

## 下一版 v2.5

讓 `series.html` 讀取 `data/stories.json`，自動依系列分組。
